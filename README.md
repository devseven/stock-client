# G&B Customer StockClient Demo

## Install

Run `npm install -g @angular/cli` ( might need admin/sudo ) 

## Running

cd into directory

Run `ng serve` for an instant dev/demo server. Navigate to `http://localhost:4200/`. 

